import "./App.css";
import Tabs from "./components/tabs";

export const App = ({ routes }) => {
  return (
    <div className="App">
      <header className="App-header"></header>
      <Tabs />
    </div>
  );
};

export default App;
